@model List<ReadGradeViewModel>
@{
    ViewData["Title"] = "Get Grades";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}

<div class="card mb-4">
    <div class="card-header d-flex align-items-center">
        <i class="fas fa-table me-1 me-2"></i>
        <h3>Grades List</h3>
    </div>
    <div class="card-body">
        <table class="table table-hover table-bordered">
            <thead>
                <tr>
                    <th>Not</th>
                    <th>Sınav Numarası</th>
                    <th>Öğrenci</th>
                    <th>Ders</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Score</td>
                        <td>@item.ExamNumber</td>
                        <td>@item.Student.FirstName @item.Student.LastName</td>
                        <td>@item.SubCourse.Code</td>
                        <td class="d-flex">
                            <a class="btn btn-warning" asp-area="Admin" asp-controller="Grades" asp-action="Details" asp-route-id="@item.Id">Detaylar</a>
                            <a class="btn btn-primary" asp-area="Admin" asp-controller="Grades" asp-action="Update" asp-route-id="@item.Id">Güncelle</a>
                            <a class="btn btn-danger" asp-area="Admin" asp-controller="Grades" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
